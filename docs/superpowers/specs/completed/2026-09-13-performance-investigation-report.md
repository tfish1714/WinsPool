# Performance Investigation Report

**Date:** 2026-09-13
**Analysis window:** 7 days (2026-09-06 to 2026-09-13)
**Project:** fishbone-wins-pool
**Spec:** docs/superpowers/specs/2026-09-13-performance-investigation-design.md

## Motivating Finding

The `winspool` Cloud Run service (region `us-east1`) has:
- `autoscaling.knative.dev/maxScale=1` — capped at exactly one instance, never scales out.
- No `autoscaling.knative.dev/minScale` annotation — sits at the Cloud Run default of 0, so the service scales to zero when idle and cold-starts on the next request.

Confirmed live via `gcloud run services describe winspool --region=us-east1 --format="value(spec.template.metadata.annotations)"` on 2026-09-13. Full annotation string: `autoscaling.knative.dev/maxScale=1;run.googleapis.com/client-name=gcloud;run.googleapis.com/client-version=559.0.0;run.googleapis.com/startup-cpu-boost=true`.

## Backend / Cloud Run

- **Request latency (2xx, 7-day window):** p50 = `41.3` ms, p95 = `984.7` ms.
- **Cold-start (container startup) latency:** p95 = `20111.6` ms (worst of the window, revision `winspool-00097-8g7`) across `13` revision-entries in the window (range 7146.9-20111.6 ms; each entry is one revision's startup event, not a count of scale-from-zero cold starts experienced by users).
- **Time at zero instances:** `0` of `112` hourly samples (`0`%) had zero running instances; max concurrent instance count observed was `2` (briefly, consistent with revision rollover overlap, not sustained scale-out — `maxScale=1` still caps steady-state capacity at one instance). Note the window returned 112 hourly points rather than the expected ~168 for a 7-day span; this was not investigated further per instructions not to force a different query shape.
- **Peak concurrent requests (any single hour):** `11.5` (hourly p95-aligned, max across the 7-day window). Note: the brief's original query used `ALIGN_MAX`, which the Cloud Monitoring API rejected (`400 INVALID_ARGUMENT: "The aligner cannot be applied to metrics with kind DELTA and value type DISTRIBUTION."`) because `run.googleapis.com/container/max_request_concurrencies` is a DELTA-kind, DISTRIBUTION-valued metric and `ALIGN_MAX` only works on GAUGE/numeric-valued metrics. Re-run with `ALIGN_PERCENTILE_95` (chosen for consistency with the Step 2 cold-start query's aligner; `ALIGN_PERCENTILE_99` would also have been valid) in place of `ALIGN_MAX`, same filter/window/`REDUCE_MAX` otherwise, which returned real data: 112 hourly points, max value `11.5`, next-highest `11.1`, `11.1`, `9.7`, `7.7`.

**Interpretation:** The data does not support "scale-to-zero cold starts are causing the p95 latency spikes" as stated. The service spent 0% of the 7-day window at zero instances — it did not actually scale to zero in this period despite having no `minScale` floor, so idle-triggered cold starts were rare-to-nonexistent here. The 13 measured startup events (7.1-20.1 seconds each) instead line up with revision deployments during the week, not traffic-driven scale-ups. Critically, those startup latencies (7-20 *seconds*) are 10-20x larger than the p95 *request* latency (985 ms), meaning a cold start — if a user request landed during one — would dominate that request's latency far beyond what shows up at p95; the fact that p95 is only ~1 second suggests either very few user requests actually hit a cold instance in this window, or Cloud Run's startup-cpu-boost plus low traffic volume kept most requests off the small number of cold windows. The 24x p50-to-p95 gap (41 ms to 985 ms) is real but likely reflects a mix of a few slow endpoints and/or the rare cold-start-affected request, not a systemic scale-to-zero problem in the current traffic pattern. Peak concurrency (11.5, hourly p95) is far below Cloud Run's default per-instance limit of 80 — nowhere close to saturating a single instance — so `maxScale=1` is **not currently a live bottleneck**: it remains a real latent risk if traffic grows substantially (e.g. a full 10-player live draft with many concurrent connections), but today's traffic pattern doesn't come close to exercising it. The cold-start theory as the primary driver of the motivating finding is not well corroborated by this window's data either.

## Firestore Usage

### Web app code paths

| Collection | Touched by | Cached? |
|---|---|---|
| `nfl_standings`, `nfl_teams`, `nfl_games`, `players`, `draft_order`, `draft_results`, `draft_order_rules` | `services/db_service.py:157` `get_collection_df()`, fetched in parallel by `services/data_service.py:153-161` `load_data()`'s `fetch_tasks` | `load_data()` (1hr TTL, `cache_service.py:118` `_CACHE_TTL_SECONDS`), keyed per-year; used by every standings/schedule/history/draft/prediction route |
| `players` | `services/db_service.py:171-176` `_get_players_df()` (used by `get_player_by_email`/`get_player_by_id` — auth login, profile, WS reauthenticate) | Warm-cache-first: reads the `load_data()` in-memory bundle for `year=None` if present, else falls back to an **uncached** direct `get_collection_df("players")` call |
| `players` | `services/push_service.py:17,40` `save_push_subscription()` / `send_push_notification()` | No cache — direct `get_db().collection("players").document(...)` get/update on every push-subscribe and every on-the-clock push send |
| `preseason_predictions` | `services/data_service.py:390` `get_preseason_predictions()` → `get_collection_df("preseason_predictions", ...)`; also written by `services/db_service.py:687,697` | **No cache — every call hits Firestore directly**, bypassing `load_data()` entirely |
| `consensus_projections` | `services/data_service.py:412` `get_consensus_projections()` → `get_collection_df("consensus_projections", ...)`; also written by `services/db_service.py:634` | **No cache — every call hits Firestore directly** |
| `draft_order` | `services/draft_service.py:67` `get_collection_df('draft_order')` (used only to determine the current season when `load_draft_state()` is called with `year=None`) | **No cache** — a raw, unfiltered collection fetch, separate from `load_data()`'s cached `draft_order` fetch |
| `draft_chat` (+ `messages` subcollection) | `services/chat_service.py:24,59-117` `post_system_message()`, `post_chat_message()`, `get_recent_messages()` | No cache — every chat/system message is a direct Firestore write; history is read once per WS connect |
| `metadata` (doc `cache_control`) | `services/data_service.py:70` (read), `services/db_service.py:126` `signal_data_update()` (write) | Read is throttled, not cached — `data_service.py`'s `_REMOTE_CHECK_INTERVAL` (60s) limits how often this doc is polled, but each poll is a live Firestore read, not served from memory |
| `metadata` (doc `prediction_config`) | `services/prediction_service.py:365,386` `load_config()`/`save_config()` | Effectively cached — `PredictionService.get_initialized()` (`prediction_service.py:308-332`) is a process-global singleton keyed by season (`_SERVICE_CACHE`), so `load_config()` only runs once per season per process lifetime |
| `metadata` (doc `draft_timer_{season}`) | `services/draft_service.py:188-201` (pick-clock read/write inside `load_draft_state()`) | No cache — direct read, and a write whenever a new pick's clock starts |
| `config` (doc `settings`) | `services/db_service.py:560-579` `get_config_settings()` — called directly in `routes/draft_routes.py:758` (every "pick" WS action) and again inside `load_draft_state()` (`draft_service.py:190`) | **No cache — CLAUDE.md notes this collection has no local pkl and always reads Firestore live**; called at least twice per pick |
| `analytics_cache` | `services/cache_service.py:26-56,59-87` `get_cached()`/`write_cache()` — read by standings/schedule analytics helpers in `services/analysis_service.py` | No cache layer of its own — one Firestore doc-get per `(analytic, year, week)` lookup; writes are `cache_builder.py`-only |
| `game_predictions` | `services/cache_service.py:158-178,210-230` `get_game_predictions()`/`write_game_predictions()`; read by `routes/standings_routes.py:193-194` (`/schedule/{year}`, every request), `routes/api_routes.py:149,236`, `routes/admin_routes.py:406,446,581,672`; full collection `.stream()` in `routes/api_routes.py:140` (`/api/predictions/accuracy`) | **No cache — every request re-reads the season's doc directly**; the accuracy endpoint additionally streams the whole collection to enumerate seasons |
| `prediction_features` | `services/cache_service.py:273-322,325-384` `get_prediction_features()`/`write_prediction_features()`; read by `routes/admin_routes.py:692,760`, `routes/api_routes.py:211,372` | No cache — direct doc-get (or query+`order_by` when no version given) per call |
| `elo_history` | `services/cache_service.py:399-441` `get_elo_history_season()`/`get_all_elo_history()`; read by `routes/prediction_routes.py:124` (admin Elo explorer) | No cache — `get_all_elo_history()` streams the **entire collection** (all seasons) on every call |
| `nn_weekly_accuracy` | `services/cache_service.py:497-535` (read helpers); read by `routes/prediction_routes.py:186` (admin ML Accuracy tab) | No cache — same shape as `elo_history`, including a full-collection `.stream()` in `get_all_nn_weekly_accuracy()` |
| `weekly_recaps` | `services/db_service.py:470-517` `save_weekly_recap()`/`get_weekly_recap()`; used only by admin recap generation/preview routes | No cache — but low-frequency, admin-only |
| `draft_results`, `draft_order`, `draft_order_rules` | `services/db_service.py:357-384` `delete_season_data()` (admin season wipe) | No cache — admin-only, not a user-facing hot path |

**Live draft WebSocket (`routes/draft_routes.py`):** Every inbound WS message that mutates or re-renders state (`pick`, `force_pick`, `undo_pick`, `reset_pick`) calls `services/draft_service.py::load_draft_state()`. That function has its own process-local singleton (`_CACHED_DRAFT_STATE`, `draft_service.py:13,48-56`) that serves a cheap in-memory `deepcopy` when `year is None` and the singleton is warm — but `save_pick()`/`undo_pick()`/`reset_pick()` each set `_CACHED_DRAFT_STATE = None` (`draft_service.py:230,236,257`), so **every single pick forces a full rebuild twice**: once to validate the incoming pick against current state, and once immediately after `save_pick()` to build the broadcast state. Each full rebuild does a direct, uncached `get_collection_df('draft_order')` scan to resolve the season (`draft_service.py:67`, only skipped if a `year` was explicitly supplied), then calls both `get_season_projection_blended()` (`draft_service.py:153`) and `get_season_projection_dual()` (`draft_service.py:159`) — which independently and redundantly each re-fetch `preseason_predictions` and `consensus_projections` uncached (4 raw Firestore reads total, not 2, since the two functions duplicate each other's work), plus an uncached `get_config_settings()` read for the pick-timer-open check (`draft_service.py:190`) and a `metadata/draft_timer_{season}` read/write (`draft_service.py:192-201`). The `pick` handler itself also calls `get_config_settings()` a second time directly (`draft_routes.py:758`) before `load_draft_state()` is even invoked. Net effect: a single pick during a live draft triggers on the order of 10+ direct, uncached Firestore round-trips (season lookup, 4x projection reads, 2x config reads, timer read/write), independent of how many of the up to 10 players are connected — the WebSocket fan-out itself (`ConnectionManager.broadcast()`) is pure in-memory `send_json` and scales fine, but the per-pick Firestore work is the part that does not obviously scale gracefully, especially given the fully serialized (30-pick) nature of a draft makes this a fixed ~300-400+ Firestore call tax per draft regardless of instance count. This is a code-level read, not a measured one — Task 4's aggregate metrics should confirm whether this shows up as real latency or Firestore cost in practice.

## Frontend Core Web Vitals

*(filled in by Task 6)*

## Recommendations

*(filled in by Task 7)*
